export type UserType = {
  _id: string;
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

export type UserGoogleType = {
  name: string;
  email: string;
  photo: string;
};
